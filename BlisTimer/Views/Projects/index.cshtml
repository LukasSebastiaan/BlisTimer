@model List<BlisTimer.Models.Project>

@{
    Layout = "_EmptyLayout";
}

@section Scripts{
    <script src="https://kit.fontawesome.com/25b6802032.js" crossorigin="anonymous"></script>
}
@section head{
    <script type="text/javascript" src="https://code.jquery.com/jquery-1.7.1.min.js"></script>
}
<div class="main">
    <div class="side-bar">
        <div class="menu">
            @foreach (var project in @Model)
            {
                <div class="item">
                    <a class="sub-btn">@project.Name <i class="fa-solid fa-angle-right"></i> </a>
                    <div class="sub-menu">
                    @foreach (var activity in project.Activities)
                    {
                        <a href="#" class="sub-item">@activity.Name</a>
                    }
                    
                    </div>
                </div>
            }
        </div>
    </div>
</div>

<script type="text/javascript">
    console.log("hello");
    $(document).ready(function (){
        $('.sub-btn').click(function(){
        $(this).next('.sub-menu').slideToggle();
        $(this).find('.fa-solid').toggleClass('rotate');
        });
    });
</script>